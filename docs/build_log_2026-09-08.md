# Build log — 2026-09-08

Owner authorization: expand JunoCam beyond PJ4 using subagents, retain strict
instrument/radiometric-failure exclusion, and push completed code/docs to
GitHub. Lead selected the exact native acquisition and adjudicated eligibility.

| Work | Executor | Measured validation / result |
| --- | --- | --- |
| Archive/cost/JIRAM/kernel audit | catalog | 337 exact preferred labels, all label hashes/times matched; 194 supported finite-timing RGB candidates, 7,854,710,784 native bytes |
| Primary instrument/timing audit | science | Normal-operation evidence plus product-specific restrictions; no broad whitelist; 337-row durable label/timing audit |
| Pipeline/preservation audit | frontend | Confirmed destructive selected-pass strip replacement, cache/locking and supported-format limits |
| Incremental strip repair | frontend, verified by lead | Fixed gate: old implementation 8 failed/1 passed; repaired implementation 9 passed; focused executor suite 57 passed |
| Exact acquisition/index/quality | catalog | 194 labels and images, all MD5/size checks pass; transfer 136.88 s, index 2.84 s, quality 462.54 s; full run including checksum/gate 637.45 s |
| Independent band triage | science, reproduced by lead | All 194 pass overall and every RGB band; seven native spot checks; no near-threshold cases |
| Full geometry | lead | 194/194 success; 629.5 s with four workers; largest-label pilot 17.77 s, 2,052,516 KiB peak RSS |
| Individual policy clearances | lead | Exact 194 IDs only; 266 preferred eligible observations including existing 72 PJ4; live joint catalog 47,925 rows |
| Offline suite after policy update | lead | 348 passed, 52 skipped, 85.22 s |
| Real-catalog matching repairs | frontend, verified by lead | Empty-outline fallback: fixed gate initially 4 failed/3 passed; paired-product physical-half attribution then reproduced 2 failed/7 passed; repaired combined gate 9 passed within the full suite |
| Final offline suite after matching repairs | lead | 357 passed, 52 skipped, 236.39 s under the concurrent mapping workload |
| Lead acquisition/preservation gate | lead | 1 passed, 61.90 s; all 194 native/label MD5 and sizes plus original image/quality rows verified |
| Existing JunoCam geometry gate | lead | 3 passed, 5.55 s |
| Existing JunoCam product/API gates | lead | 3 passed, 213.63 s; the previous aggregate catalog-count conflict is resolved by the expanded eligible sample, with the gate unchanged |
| New mapped products through API | lead | Eight HTTP 200 requests: three PJ5 physical-band PNGs, stack listing, PJ34 physical-band/RGB PNGs, RED readiness and native RED statistics; statistics cold request 125.36 s; separate PJ58 thumbnail 403 and unavailable-band 404 checks passed |
| Expanded catalog in production browser | lead | All 266 eligible JunoCam observations and exact counts for all eight added passes verified through the instrument/pass controls; rendered coverage reviewed; temporary server/browser closed. This checks catalog interaction, not completion of every background-loaded view |
| JIRAM candidate audit | science, verified by lead | Same 194 requests/catalog/policy before and after matching repairs: 18→174 observations with candidates; 140 with physical M candidates; 2,639 pairs; no request capped. Lead independently recounted responses and reproduced eight current API examples |
| Dense strips | catalog, independently verified by lead | 102/102 across eight passes, zero failures, 26,926,789,342 bytes; 92 coarser eligible observations not stripped; every saved header/mask fraction and physical-band signal checked |
| Polar stacks | lead; independent PJ34 audit by science | 13 files, 62 distinct source observations, 5,295,391,665 bytes; all saved masks/bands/times and RED readiness independently recomputed; seven runs in four northern stacks |
| Final expansion integration gates | lead | 2 passed, 121.22 s; with the prior acquisition gate, all three fixed expansion checks pass; all 320 old NetCDF sizes/mtimes preserved |
| Final catalog/index inventory | lead | All 305 historical strip-index rows unchanged; 407 stored rows, 399 GUI rows including 110 preferred JunoCam strips; 18 API stacks including all 13 new stacks |
| Completed products in production browser | lead | Image library shows 110 of 399 strips and renders a new PJ34 physical RED image; Time series renders PJ34 north RED/native, ten distinct observations and the two measured cadence runs; saved screenshots and state records retained; owned servers/browser closed |
| Limb-refinement outcomes | lead | 126 distinct mapped sources: 103 successful fits, 23 below the 200-point minimum with nominal timing retained; cache/strip fitted offsets agree including explicit missing values; per-source navigation CSV and per-run fit status recorded |
| Guide and pedagogical review/deck | frontend, reviewed by lead | Final measured counts, source/refinement/cadence distinctions and matching repairs incorporated; rebuilt 110-page PDF, 2,181,129 bytes, no LaTeX warnings or over/underfull boxes; links, fences and figure references pass |

The first geometry preservation assertion compared disk-loaded historical
strings to the in-memory coerced table and failed on the pandas NA dtype
representation. A direct saved-table comparison then proved the 122 old rows
identical, including dtypes. No geometry rerun or numerical change was needed;
the operational verifier now compares persisted tables.

Two final operational audit assumptions were corrected after inspecting the
saved files: unfitted timing offsets intentionally store `NaN` in both cache
and strip, and single-digit pass names in stack IDs are not zero-padded.
The verifier now compares the missing-fit states explicitly and takes exact
IDs from the saved product manifest. Both checks passed without changing
application code, scientific products or the fixed integration gates. The
initial diagnostic logs remain alongside the corrected audit outputs.

The first final browser attempt verified Image library, then tried to select
normalization inside a closed disclosure control. The follow-up opened
**Advanced illumination and display stretch**, selected native RED and
verified the Time series image and readiness results. This corrected browser
automation only; no application change was required. The two successful view
checks, initial diagnostic and screenshots are retained under the expansion
audit, with the screenshots in `evidence/final_browser/`.

Dense strip reprojection proved much slower than metadata geometry. Processing
therefore uses a measured pilot, a bounded product worker pool and independent
pass scheduling. Atomic per-pass directory locks keep strip/polar jobs from
writing the same fitted-geometry cache concurrently; shared strip-index writers
stay serialized. These operational locks do not claim a new application-wide
locking implementation. Resource decisions are in the accepted execution specs.

The largest measured strip-process tree used 41.35 GiB; the largest individual
worker high-water was 7.084 GiB. The final runner verified BLAS/OMP/MKL limits
of one and Arrow CPU/I/O pools of four in all 56 spawned workers. Earlier
PJ6 workers inherited broader numerical-library defaults; two controlled
between-pass restarts preserved completed products and corrected that setup.
The startup memory projection used the PJ6 pilot and was not retrospectively
increased during later passes. Future sizing at 1.25 times the final measured
worker maximum needs 8.855 GiB per worker, allowing nine workers within the
84 GiB strip pool. All workers, temporary strip files and pass locks were
gone after completion. Timings and these operational qualifications are
retained in `strip_run_summary.json` and `strip_completion_audit.json`.

Evidence, exact acquisitions and individual results are linked from the
[expansion record](reports/junocam_expansion_2026-09-08.md). Native and derived
scientific data remain in the external mirror, outside Git.

Judgment calls: used the existing three subagents for independent bounded work;
kept acquisition/eligibility/physical-model decisions with the lead; corrected
the descriptive throughput interpretation without rewriting legacy numeric
tables; preserved old products; expanded a measured RGB sample and retained
explicit navigation, response and cadence limits.
