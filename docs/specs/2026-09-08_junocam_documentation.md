# Accepted multi-pass guide and teaching-note refresh

Parent accepted expansion includes current documentation and preserved GUI.
Executor owns only docs/gui_guide.md, PEDAGOGICAL_REVIEW.md,
docs/pedagogy/slides.tex and its regenerated slides.pdf. Existing screenshots
remain unchanged and retain their actual September7capture dates. All code,
gates, fixtures, specs and mirror files are read-only. Root owns other docs.

Update the relevant paragraphs to teach exact-product acquisition, individual
health+signal+navigation assessment, preferred-version identity and the repaired
additive strip-index behavior. Explain the eight-pass bounded sample and
mission-long native-response/compression/illumination caveats without suggesting
instrument failure is allowed or that raw cross-pass brightness is invariant.
Existing numeric throughput factors retain their legacy display domain; missing
earlier factors do not establish unity. No need to restate the full audit.

Final local-data counts, mapped/stack coverage and cadence must come from the
root's completed verification report, not intended194acquisitions. Draft the
known methodological text now, and wait for root's measured counts before
completing the snapshot. Link docs/reports/junocam_expansion_2026-09-08.md and
the three reconnaissance reports as useful. Keep historical examples dated;
PJ4's three-image irregular-cadence example remains true for that stack.

Revise only directly stale teaching/slides passages. If slides change, rebuild
the PDF using the repository's existing method with intermediate files in
Lustre TMPDIR; never claim a rebuild that did not occur. Validate internal
links, markdown syntax, LaTeX compilation where relevant and git diff --check.
Report exact file scope, validation and any unresolved facts to the lead.

Judgment calls: retain screenshots because the controls are unchanged; update
the data snapshot separately; preserve historical examples as labelled examples.

If any part of this spec is ambiguous or underdetermined, do NOT choose an interpretation. Stop, list the ambiguities and the options, and make no further changes. Also list every judgment call you made, however minor, at the end of your final message.
