[project]
name = "jiram-catalog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "certifi>=2026.7.22",
    "fastapi>=0.141.1",
    "imageio>=2.37.4",
    "imageio-ffmpeg>=0.6.0",
    "matplotlib>=3.11.1",
    "netcdf4>=1.7.4",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "pvl>=1.3.2",
    "pyarrow>=25.0.1",
    "pyyaml>=6.0.3",
    "spiceypy>=8.2.0",
    "uvicorn[standard]>=0.52.4",
    "xarray>=2026.7.0",
]

[project.scripts]
jiram-catalog = "jiram_catalog.cli:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
